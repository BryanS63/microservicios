server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          defautl-label: eureka
          uri: https://github.com/BryanS63/microservicios.git
          search-paths: config-data
  application:
    name: config-service